{% extends 'base.html.twig' %}
{# /**
 * @author Mohammed MOQRAN 
 */ #}
{% block title %}Hello ProjectManagerController!{% endblock %}

{% block body %}
<table class="table">
  <thead>
  
        <tr>
        <th scope="col">#</th>
        <th scope="col">Titre</th>
        <th scope="col">Description</th>
        <th scope="col">Date début</th>
        <th scope="col">DeadLine</th>
        <th scope="col">Date fin</th>
        <th scope="col">modifier</th>
        </tr>
    </thead>
    {% for project in projects %}

    
    <tbody>
        <tr>
        <th scope="row">1</th>
        <td>{{project.title}}</td>
        <td>{{project.description}}</td>
        <td>{{ project.dateInit|date("m/d/Y")}}</td>
        <td>{{ project.DeadLine|date("m/d/Y")}}</td>
        <td>{{ project.dateFin|date("m/d/Y")}}</td>
        </tr>
    </tbody>

    {% endfor %}
</table>

{% endblock %}
